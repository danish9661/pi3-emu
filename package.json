{
  "name": "pi-emu-spike",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "vite",
    "build": "bash build.sh && vite build",
    "preview": "vite preview",
    "smoke": "node test/pi-cpu-smoke.mjs"
  },
  "dependencies": {
    "puppeteer-core": "^25.8.0"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "vite": "^6.0.0"
  }
}
