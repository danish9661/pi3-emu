[workspace]
resolver = "2"
members = ["runtime", "clock", "shell", "sum", "fib", "smp", "gpio", "fb", "irq", "mmu", "dma", "pwm"]

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
